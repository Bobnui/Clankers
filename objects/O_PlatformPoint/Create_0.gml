if (StartingPoint) {
	PlatformID.startX = x;				// if start point, create variables in assigned platform instance and set to X and Y
	PlatformID.startY = y;
}
else {
	PlatformID.endX = x;				// if end point, create variables in assigned platform instance and set to X and Y
	PlatformID.endY = y;
}